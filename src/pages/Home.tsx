import React from "react";
import FormContainer from "../components/FormContainer";

const Home = () => {
    return (
        <main>
            <FormContainer />
        </main>
    );
}

export default Home;