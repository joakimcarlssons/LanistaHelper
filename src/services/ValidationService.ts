// import { RequirementType } from "../data/Enums";
// import Race from "../models/Race";
// import Weapon from "../models/Weapon_obs";

// class Validator {
//     validateRace(weapon: Weapon, selectedRace: Race | null | undefined) : boolean {
//         const raceReqs = weapon?.requirements?.filter(req => req.requirementable === RequirementType.Race);
//         if (!raceReqs || raceReqs.length == 0) return false;
//         return !(raceReqs?.some(req => req.race_name?.toLowerCase() === selectedRace?.swe_name.toLowerCase()) && raceReqs.length > 0)
//     }
// }

// export default new Validator(); 

export default "";